export type Shop = {
  addressLine: string;
  city: string;
  name: string;
  telephone: string;
  thumbnail: string;
  workingHour: string;
  zipCode: string;
};

export type States = {
  name: string;
  state: string;
  uniqueID: string;
};

export type Cities = {
  name: string;
  uniqueID: string;
};
